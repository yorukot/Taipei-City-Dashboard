name: Website CI

on:
  push:
    paths:
      - "website/**"
      - ".github/workflows/frontend.yml"
  pull_request:
    paths:
      - "website/**"
      - ".github/workflows/frontend.yml"

permissions:
  contents: read

jobs:
  checks:
    name: Prettier and Build
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: Taipei-City-Dashboard-FE

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up pnpm
        uses: pnpm/action-setup@v4
        with:
          package_json_file: Taipei-City-Dashboard-FE/package.json

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: pnpm
          cache-dependency-path: Taipei-City-Dashboard-FE/pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Check formatting
        run: pnpm exec prettier --check .

      - name: Build website
        run: pnpm build
